---
pre-commit:
  parallel: true
  commands:
    gitleaks:
      run: gitleaks detect --source . --no-banner --log-level=error --no-git
    lint:
      glob: "*.go"
      run: make lint
    fmt:
      glob: "*.go"
      run: make fmt

pre-push:
  commands:
    test:
      run: make test
